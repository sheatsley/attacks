apgdce is actually surpasing semantic tests now (we are 0.2% better than pytorch now). this includes removing early termination logic from torchattacks. only restore to this commit if you need to confirm basic operations. the repo will be cleaned up afterwards.
